# Google OAuth credentials (required for dashboard login and Gmail connections)
# Create at: https://console.cloud.google.com/apis/credentials
# See README for step-by-step setup instructions
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/callback

# Server config
BASE_URL=http://localhost:3000
PORT=3000

# Session secret (required, 32+ characters)
# Used for HMAC-signing OAuth state and session cookies
SESSION_SECRET=change-me-to-a-random-string-at-least-32-chars

# Session lifetime (optional — defaults to 7 days in ms)
# SESSION_MAX_AGE=604800000

# Database (optional — defaults to data/agentcloak.db)
# DATABASE_PATH=data/agentcloak.db

# Database encryption key (optional — requires better-sqlite3-multiple-ciphers
# instead of standard better-sqlite3. Without it, this setting is a no-op.)
# DATABASE_ENCRYPTION_KEY=your-encryption-key
