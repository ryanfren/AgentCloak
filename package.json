{
  "name": "agentcloak",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source proxy that sits between AI agents and email, filtering sensitive content",
  "scripts": {
    "dev": "pnpm --filter @agentcloak/server dev",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  },
  "engines": {
    "node": ">=20"
  }
}
